module.exports = function sleep (ms, async, cb) {
    if (async) {
        setTimeout(cb, ms);
    } else {
        var startTime = new Date();
        while (true) {
            if (new Date() - startTime > ms) { break }
        }
        return cb()
    }
};
