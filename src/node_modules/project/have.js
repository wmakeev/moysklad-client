var isUuid = require('project/tools/isUuid')

module.exports = require('have2').with({
    'Entity': function (entity) {
        return entity && entity.TYPE_NAME
    },
    'UUID': function (uuid) {
        return isUuid(uuid)
    }
}).strict


